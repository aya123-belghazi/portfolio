
  # Portfolio de développeur Full Stack

  This is a code bundle for Portfolio de développeur Full Stack. 
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
